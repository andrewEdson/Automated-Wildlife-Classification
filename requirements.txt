# Install PyTorch for Apple Silicon (MPS) from https://pytorch.org/get-started/locally/
# Select: Stable | Mac | Pip | Python | Default
# Example (check pytorch.org for the latest command):
#   pip install torch torchvision
#
# For CUDA (Linux/Windows with NVIDIA GPU), select the appropriate CUDA version.

torch
torchvision

# Scientific computing
numpy
matplotlib
seaborn

# Machine learning utilities
scikit-learn

# Image processing
Pillow

# Progress bars
tqdm

# Jupyter notebook
jupyter
ipywidgets

# MegaDetector (Phase 1: animal detector for crop pipeline)
# NOTE: PytorchWildlife pulls in yolov5 which uses pkg_resources;
#       pin setuptools<81 to keep that import alive.
PytorchWildlife
soundfile
librosa
setuptools<81
